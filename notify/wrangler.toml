name = "turcan-cv-notify"
main = "worker.js"
compatibility_date = "2025-01-01"

[vars]
ALLOWED_ORIGINS = "https://turcan.nl,https://www.turcan.nl,http://localhost:8000,http://127.0.0.1:8000"

# Secrets (do NOT put here, set with `wrangler secret put`):
#   TELEGRAM_BOT_TOKEN
#   TELEGRAM_CHAT_ID
